# Variables de entorno para el frontend
# En producción, estas se configuran en Vercel

# URL del backend (en desarrollo)
VITE_API_URL=http://localhost:3000

# En producción, Vercel automáticamente usará la URL del deploy
# VITE_API_URL=https://tu-proyecto.vercel.app

# INSTRUCCIONES:
# 1. Copia este archivo como .env en la carpeta client/
# 2. O ejecuta: copy env.example .env (en Windows)
# 3. O ejecuta: cp env.example .env (en Linux/Mac)
